import { LocaleMessage } from "@intlify/core-base";
import { VueMessageType } from "@nuxtjs/i18n/dist/runtime/composables";

const messages: LocaleMessage<VueMessageType> = {
  projects: {
    title: "Projects",
  },
};

export default messages;
